import { TiempoAtrasPipe } from './tiempo-atras.pipe';

describe('TiempoAtrasPipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoAtrasPipe();
    expect(pipe).toBeTruthy();
  });
});
